1. add radzen notification when a newer version is available (how to detect versions, should we parse github releases web pages?, do github offers an API it would be the best? the notification could have a link to directly download the new version)
2. add a splash screen with the app logo and version number while loading the app (maybe also a progress bar, but it could be tricky to implement) 
3. review app strings, like settings explaining too much
4. clean up documentation (readme must be more attractive and professional (why this app, screenshots, use cases), overview needs a huge clean up, roadmap should be deleted while maybe keeping some valuable information somewhere else)
5. clean git commit history, remove unnecessary files
6. make repo public and configure permissions
