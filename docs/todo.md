1. implement more unit tests, should some classes have interfaces to allow more unitestability ?
2. full code review and refactor to improve maintainability and readability
3. taks scheduler
4. create new task from a list of saved tasks (reusable tasks)
5. attach files to a task prompt and send them when running the task in the terminal, file picker, drag and drop, or copy paste (binary data) must be supported 
