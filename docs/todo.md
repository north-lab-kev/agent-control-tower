1. attach files to a task prompt and send them when running the task in the terminal, file picker, drag and drop, or copy paste (binary data) must be supported 
2. Serilog with microsoft.extensions.logging: add log files to a temp act folder if not already done, log error when retriving the usage, etc... from the settings page have a button to open the log folder in the file explorer
3. review app strings, like settings explaining too much
4. check error message in usage tiles, ask to replace by a not available right now and show the reason only in the tooltip?
5. new setting: Help improve the project by sending anonymous usage data and crash reports (opt-out)
6. usage metric with PostHog
7. worktree support (claude cli have it natively, codex require wrappers like agentree or nymbalyst)
