for me, not the agent

always use code behing cs file instead of directly in razor

should use most of the time radzen controls instead of html controls, it should leeds to use way less css styles and more consistent UI
