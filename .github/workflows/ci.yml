name: ci

# Temporarily disabled — manual-only until the real build + fast test suite
# lands (roadmap step 2). Then set `on: [push, pull_request]`.
#
# While the repo is PRIVATE: run Linux-only on push/PR, Windows occasionally
# (Windows drains the free tier at 2x). Switch on the full Windows + Linux
# matrix at go-public. Include the Playwright browser-install step.
on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI not configured yet - see roadmap step 2."
