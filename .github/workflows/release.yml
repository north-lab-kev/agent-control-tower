name: release

# Temporarily disabled — manual-only until the release build lands
# (roadmap step 2). Then trigger on version tags:
#   on:
#     push:
#       tags: ['v*']
on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release not configured yet - see roadmap step 2."
